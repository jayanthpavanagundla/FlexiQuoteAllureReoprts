# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests\globalSearch.spec.ts >> Global Search >> Validate Search by Customer name
- Location: tests\globalSearch.spec.ts:63:9

# Error details

```
Error: expect(locator).toHaveValue(expected) failed

Locator: locator('//label[contains(.,\'Make / Model\')]/following-sibling::*//input').first()
Expected pattern: /^\s*FORD Capri\s*$/
Received string:  "FORD CAPRI DLUX"
Timeout: 8000ms

Call log:
  - Expect "soft toHaveValue" with timeout 8000ms
  - waiting for locator('//label[contains(.,\'Make / Model\')]/following-sibling::*//input').first()
    19 × locator resolved to <input type="text" data-v-18fe450a="" autocomplete="off" aria-autocomplete="list" icon="material-symbols-rounded" class="input is-required-field"/>
       - unexpected value "FORD CAPRI DLUX"

```

```yaml
- textbox: FORD CAPRI DLUX
```

```
Error: expect(locator).toHaveValue(expected) failed

Locator:  getByPlaceholder('Year')
Expected: "2010"
Received: "1969"
Timeout:  8000ms

Call log:
  - Expect "soft toHaveValue" with timeout 8000ms
  - waiting for getByPlaceholder('Year')
    19 × locator resolved to <input type="text" class="input" data-v-fc012cdc="" placeholder="Year" content="Required by ORM."/>
       - unexpected value "1969"

```

```yaml
- textbox "Year": "1969"
```

# Test source

```ts
  48  |     async getSuggestionQuoteNumbers(): Promise<string[]> {
  49  |         await this.suggestionItems.first().waitFor({ state: 'visible' });
  50  |         const count = await this.suggestionItems.count();
  51  |         const quoteNumbers: string[] = [];
  52  |         for (let i = 0; i < count; i++) {
  53  |             const text = await this.suggestionItems.nth(i).innerText();
  54  |             const match = text.match(/#(\d+)/);
  55  |             if (match) quoteNumbers.push(match[1]);
  56  |         }
  57  |         return quoteNumbers;
  58  |     }
  59  | 
  60  |     async openRandomSuggestion(): Promise<string> {
  61  |         let selectedQuoteNumber = '';
  62  |         await step('Open a random quote from search suggestions', async () => {
  63  |             const count = await this.suggestionItems.count();
  64  |             const idx = Math.floor(Math.random() * count);
  65  |             const item = this.suggestionItems.nth(idx);
  66  |             const text = await item.innerText();
  67  |             selectedQuoteNumber = text.match(/#(\d+)/)?.[1] ?? '';
  68  |             await item.click();
  69  |         });
  70  |         return selectedQuoteNumber;
  71  |     }
  72  | 
  73  |     async openRandomSuggestionAndCaptureDetails(): Promise<SuggestionDetails> {
  74  |         let details: SuggestionDetails = {
  75  |             registration: '', quoteNumber: '', status: '',
  76  |             buildYear: '', vehicleMakeModel: '',
  77  |             customerName: '', phone: '',
  78  |             colour: '', claimNumber: '',
  79  |             insurer: '', assessor: '',
  80  |         };
  81  |         await step('Capture suggestion details and open random quote', async () => {
  82  |             await this.suggestionItems.first().waitFor({ state: 'visible' });
  83  |             const count = await this.suggestionItems.count();
  84  |             const idx = Math.floor(Math.random() * count);
  85  |             const item = this.suggestionItems.nth(idx);
  86  | 
  87  |             details = await item.evaluate((el: HTMLElement): SuggestionDetails => {
  88  |                 const txt = (sel: string): string =>
  89  |                     (el.querySelector(sel)?.textContent?.trim() ?? '').replace(/\s+/g, ' ');
  90  | 
  91  |                 const val = (label: string): string => {
  92  |                     const lbl = Array.from(el.querySelectorAll('.s-lbl')).find(
  93  |                         e => e.textContent?.trim() === label
  94  |                     );
  95  |                     const v = (lbl?.nextElementSibling as HTMLElement)
  96  |                         ?.textContent?.trim().replace(/\s+/g, ' ') ?? '';
  97  |                     return v === '-' ? '' : v;
  98  |                 };
  99  | 
  100 |                 const vehicleText = txt('.s-name');
  101 |                 const vehicleMatch = vehicleText.match(/^(\d{4})\s+(.+)$/);
  102 | 
  103 |                 return {
  104 |                     registration:    txt('.s-car-reg'),
  105 |                     quoteNumber:     txt('.s-quote').replace('#', '').replace(/\s/g, ''),
  106 |                     status:          txt('.s-status'),
  107 |                     buildYear:       vehicleMatch?.[1] ?? '',
  108 |                     vehicleMakeModel: (vehicleMatch?.[2] ?? vehicleText).trim(),
  109 |                     customerName:    val('Customer:'),
  110 |                     phone:           val('Ph:'),
  111 |                     colour:          val('Colour:'),
  112 |                     claimNumber:     val('Claim:'),
  113 |                     insurer:         val('Insurer:'),
  114 |                     assessor:        val('Assessor:'),
  115 |                 };
  116 |             });
  117 |             await item.click();
  118 |         });
  119 |         return details;
  120 |     }
  121 | 
  122 |     async validateQuoteDetailsMatch(details: SuggestionDetails): Promise<void> {
  123 |         await step('Validate quote detail page matches suggestion card', async () => {
  124 |             const quotePage = new QuotePage(this.page);
  125 | 
  126 |             if (details.quoteNumber) {
  127 |                 const baseNumber = details.quoteNumber.split('/')[0];
  128 |                 const regex = new RegExp(`^\\s*${baseNumber}(\\/\\d+)?\\s*$`);
  129 |                 await expect
  130 |                     .soft(quotePage.quoteNumber.filter({ hasText: regex }).first())
  131 |                     .toBeVisible({ timeout: 8000 });
  132 |             }
  133 |             if (details.registration) {
  134 |                 const escapedRego = details.registration.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  135 |                 await expect
  136 |                     .soft(quotePage.regoInput)
  137 |                     .toHaveValue(new RegExp(`^${escapedRego}$`, 'i'), { timeout: 8000 });
  138 |             }
  139 |             if (details.vehicleMakeModel) {
  140 |                 const expected = details.vehicleMakeModel.replace(/\s+/g, ' ').trim();
  141 |                 await expect
  142 |                     .soft(quotePage.makeModelInput)
  143 |                     .toHaveValue(new RegExp(`^\\s*${expected.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')}\\s*$`), { timeout: 8000 });
  144 |             }
  145 |             if (details.buildYear) {
  146 |                 await expect
  147 |                     .soft(quotePage.buildYearInput)
> 148 |                     .toHaveValue(details.buildYear, { timeout: 8000 });
      |                      ^ Error: expect(locator).toHaveValue(expected) failed
  149 |             }
  150 |             if (details.customerName) {
  151 |                 const [lastName, firstName] = details.customerName.split(', ');
  152 |                 if (lastName) await expect.soft(quotePage.lastName).toHaveValue(lastName, { timeout: 8000 });
  153 |                 if (firstName) await expect.soft(quotePage.firstName).toHaveValue(firstName, { timeout: 8000 });
  154 |             }
  155 |             if (details.phone) {
  156 |                 await expect
  157 |                     .soft(quotePage.phoneInput)
  158 |                     .toHaveValue(details.phone, { timeout: 8000 });
  159 |             }
  160 |             if (details.colour) {
  161 |                 await expect
  162 |                     .soft(quotePage.colorInput)
  163 |                     .toHaveValue(details.colour, { timeout: 8000 });
  164 |             }
  165 |             if (details.claimNumber) {
  166 |                 await expect
  167 |                     .soft(quotePage.claimNumberInput)
  168 |                     .toHaveValue(details.claimNumber, { timeout: 8000 });
  169 |             }
  170 |             if (details.insurer) {
  171 |                 await expect
  172 |                     .soft(quotePage.selectedInsurer)
  173 |                     .toContainText(details.insurer, { timeout: 8000 });
  174 |             }
  175 |             if (details.assessor) {
  176 |                 await expect
  177 |                     .soft(quotePage.assessorInput)
  178 |                     .toHaveValue(details.assessor, { timeout: 8000 });
  179 |             }
  180 |             
  181 |         });
  182 |     }
  183 | 
  184 |     async expectNoSuggestionsVisible(): Promise<void> {
  185 |         await expect(this.suggestionItems).toHaveCount(0, { timeout: 5000 });
  186 |     }
  187 | 
  188 |     async typeSearchAndValidateApi(query: string): Promise<boolean> {
  189 |         let hasResults = false;
  190 |         await step(`Search "${query}" and validate against Typesense API`, async () => {
  191 | 
  192 |             const responsePromise = this.page.waitForResponse(
  193 |                 r =>
  194 |                     r.url().includes('typesense.net') &&
  195 |                     r.url().includes('/search') &&
  196 |                     !r.url().includes('q=*') &&
  197 |                     !r.url().includes('q=%2A') &&
  198 |                     r.request().method() === 'GET',
  199 |                 { timeout: 20000 }
  200 |             );
  201 | 
  202 |             await this.typeSearch(query);
  203 |             const response = await responsePromise;
  204 | 
  205 |             // Non-200 (e.g. 400 for too-short query, 422, 5xx) → treat as no results
  206 |             if (response.status() !== 200) {
  207 |                 await this.expectNoSuggestionsVisible();
  208 |                 hasResults = false;
  209 |                 return;
  210 |             }
  211 | 
  212 |             const body = (await response.json()) as {
  213 |                 found: number;
  214 |                 hits: { document: Record<string, unknown> }[];
  215 |             };
  216 |             const apiFound = body.found;
  217 | 
  218 |             // Validation found === 0 → dropdown must not be shown
  219 |             if (apiFound === 0) {
  220 |                 await step(`API found=0 → dropdown must not be shown`, async () => {
  221 |                     await this.expectNoSuggestionsVisible();
  222 |                 });
  223 |                 hasResults = false;
  224 |                 return;
  225 |             }
  226 | 
  227 |             // Validation found > 0 → dropdown must be shown
  228 |             await step(`API found=${apiFound} → dropdown must be shown`, async () => {
  229 |                 await this.expectSuggestionsVisible();
  230 |                 const uiCount = await this.getSuggestionQuoteNumbers().then(q => q.length);
  231 |                 expect(
  232 |                     uiCount,
  233 |                     `API found ${apiFound} result(s) for "${query}", so the dropdown ` +
  234 |                         `must show at least one option, but none were shown`
  235 |                 ).toBeGreaterThan(0);
  236 |             });
  237 |             hasResults = true;
  238 |         });
  239 |         return hasResults;
  240 |     }
  241 | 
  242 |     async validateSearchFieldOnQuote(fieldLabel: string, searchValue: string): Promise<void> {
  243 |         const quotePage = new QuotePage(this.page);
  244 | 
  245 |         if (fieldLabel === 'VIN Number') {
  246 |             await step(`Validate VIN contains "${searchValue}"`, async () => {
  247 |                 const actual = await quotePage.vinInput.inputValue();
  248 |                 const escaped = searchValue.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
```